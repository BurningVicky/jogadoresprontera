services:
  - type: web
    name: jogadores-api
    env: docker
    plan: free
    autoDeploy: true
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      DATABASE_URL: jogadores.db
    ports:
      - port: 10000
        protocol: http
